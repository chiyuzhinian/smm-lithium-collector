# 部署状态 2026-09-09 — V5 前端业务升级 + V6 功能修复优化

> 状态：**V5+V6 代码全部完成并 8899 预览验收通过（pytest 253 / 浏览器断言 234 项全过）；
> 生产 8888 待用户确认后重启（`systemctl restart smm-fileserver`）。**

## V6 追加变更（2026-09-09 晚）

1. **暂无数据文案统一**：「暂无独立报价」→「暂无数据」（每日报价标签/空单元格/详情、
   月报完整性标签、后端 fallback、映射 note 前缀）；真实无报价逻辑保留（行留空、环比不算）
2. **价格走势**：产品下拉去掉 10 条上限（空输入列出全部 40 条首页产品，320px 滚动）；
   点击输入框重渲染（修复点选后列表不刷新）；>10 产品黄金角扩展配色（顺序分配槽位不撞色）；
   图例 max-height 104px 滚动；5 产品 → 5 条独立曲线已在预览实测
3. **数据与报表重构**：dataset tab → 「价格对比」（首页 40 产品多选 × 日期范围 →
   /api/portal/history 真实报价点对比表：日期/产品/规格/最低/最高/日均价/单位，初始自动
   加载全部产品）；**删除历史归档**：/history 页面、入口按钮、分页条、历史汇总/固定汇总链接；
   保留 /api/history、/api/categories 端点与全部 DB/归档数据
4. 验收：pytest 253 passed；Playwright 234 项全过（新增：下拉全量 40 条、11+ 产品不撞色、
   图例滚动、跨单位对比、无分页、无归档入口）；?v=6 缓存

## 一、本轮变更（V5）

1. **华友信息清理（仅展示层）**
   - 品牌改名：页面标题×8 / 登录页 h1 / 导航品牌 / 英文副标 / 页脚 →「锂电价格与回收业务数据中心」
   - 配置：categories_portal.yaml（portal.title、company 删除、E组/回收专题描述）；
     business_products.yaml 组织值 → 回收循环板块/正极材料板块/电芯电池板块/三元材料板块、
     来源「江苏华友客户反馈」→「江苏客户反馈」；business_report_mapping.yaml 同步
   - 月报文件名 → `月度业务报表_{month}.xlsx`（下载文件名/CLI 输出同步）；
     磁盘旧文件已 mv 重命名
   - 采集原始数据（data/raw）与 SQLite 业务数据**未动**
2. **每日报价**：删除组织下拉；分类下拉前移（含新「基础金属」= A00铝/1#电解铜/1#电解镍）；
   新增产品多选下拉（chips 跨类别累积）；主列改为 SMM 原始产品名+规格（业务名作副行小字）
3. **价格走势**：点选多产品不限数量；调色板 10 色；≤2 单位自动双 Y 轴（>2 提示不可比）；
   图例带单位、空数据标「暂无历史价格」；图表高度随系列数自适应；默认空选择引导
4. **数据与报表**：分类多选（后端 dataset 接口新增 `categories` 参数）；
   月报月份下限由 DB 历史起点驱动（去硬编码）；新增 8 月报表数据校验脚本
   `scripts/verify_monthly_report.py`（报告：docs/月度报表验证_2026-08.md）
5. **视觉**：V3 深色 → V5 蓝白浅色（app.css 令牌整体替换 + trends.js 图表常量 + 403 页）；
   缓存版本 ?v=4 → ?v=5；规范附录 docs/portal-visual-spec-v5-light.md

## 二、验收结果

| 项 | 结果 |
|----|------|
| pytest | **253 passed / 0 failed**（基线 245+4 失败；新增 dataset 多分类、基础金属类别、月报 49 行守卫测试） |
| Playwright 回归（8899） | **214 项全过 / 0 失败**（4 视口×4 页 + 125% 缩放 + 月报 Excel 下载校验 + 新交互断言） |
| 8 月报表校验 | 49 行全部核对：周频/点数相同行**完全一致**（验证计算口径无偏差）；32 行差异=DB 含补采晚到点而 8 月 CSV 快照不含（固有口径差，原因已逐日核实，未改数据） |

## 三、待办（等用户确认）

1. **生产部署**：`systemctl restart smm-fileserver`（8888）→ 冒烟（/health、登录、月报下载文件名）
2. **账号改名（可选，待用户决定）**：普通账号 `huayou` 登录后右上角 chip 显示「huayou」（拼音）。
   如需彻底无华友痕迹可改名（如 `market`），改后会强制所有现有用户重新登录。
   脚本 DEFAULT_USERS 已预留同步点。
3. CLAUDE.md 待生产部署后更新快照

## 四、回滚

- 代码/配置：`git reset --hard 438c9fa`（V5 前 checkpoint）
- 磁盘文件：`mv data/exports/月度业务报表/月度业务报表_2026-08.xlsx 华友月度业务报表_2026-08.xlsx`
- 服务：`systemctl restart smm-fileserver`；8899 预览 kill+原命令重启
